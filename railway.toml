[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["server/**", "processing/**", "pyproject.toml", "Dockerfile", "railway.toml"]

[deploy]
startCommand = "timeout 30 alembic upgrade head; uvicorn server.main:app --host 0.0.0.0 --port ${PORT:-8080}"
