# Auto-generated from pyproject.toml dependencies.
# For Railway / pip-based deployments.

# Use CPU-only PyTorch wheels — Railway has no GPU, and we only need
# torch for Silero VAD (CPU-only).  This avoids downloading ~2.7 GB of
# NVIDIA CUDA packages and cuts build time from ~18 min to ~5 min.
--extra-index-url https://download.pytorch.org/whl/cpu

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Task queue
celery[redis]>=5.3.6
redis>=5.0.0

# Audio analysis
imageio-ffmpeg>=0.4.9
librosa>=0.10.1
soundfile>=0.12.0
numpy>=1.26.0
scipy>=1.12.0

# Silero VAD (CPU-only inference — no GPU/CUDA needed)
torch>=2.1.0
torchaudio>=2.1.0  # Silero VAD's utils_vad.py imports torchaudio at module level

# Speech-to-text (Groq-hosted Whisper)
groq>=0.4.0

# Music score parsing
music21>=9.1.0

# NLP / entity extraction
rapidfuzz>=3.6.0

# LLM
anthropic>=0.40.0

# Config
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Auth (Supabase JWT verification)
PyJWT[crypto]>=2.8.0
httpx>=0.26.0

# Email
sendgrid>=6.11.0
